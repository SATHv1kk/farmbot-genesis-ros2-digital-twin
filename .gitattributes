# Normalize text files to LF in the repository (working copy lives on NTFS,
# where tools occasionally rewrite files with CRLF endings).
* text=auto eol=lf

# Binary assets
*.stl binary
*.pdf binary
*.png binary
*.jpg binary
