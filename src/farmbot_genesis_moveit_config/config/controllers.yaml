controller_names:
  - gantry_controller

gantry_controller:
  type: FollowJointTrajectory
  action_ns: follow_joint_trajectory
  default: true
  joints:
    - x_axis_joint
    - y_axis_joint
    - z_axis_joint
